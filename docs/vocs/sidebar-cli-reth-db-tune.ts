import { SidebarItem } from "vocs";

export const rethDbTuneCliSidebar: SidebarItem = {
    text: "reth-db-tune",
    link: "/cli/reth-db-tune",
    collapsed: true,
    items: [
        {
            text: "reth-db-tune migrate",
            link: "/cli/reth-db-tune/migrate"
        }
    ]
};
