[package]
name = "reth-tests"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/foundry-rs/reth"
readme = "README.md"

[dependencies]
reth-primitives = { path = "../primitives" }
reth-db = { path = "../db" }

serde = "1.0"
test-fuzz = "3.0.4"

